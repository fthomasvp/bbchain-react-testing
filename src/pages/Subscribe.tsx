import { SubscribeForm } from "../features/subscribe";

const Subscribe = () => {
  return (
    <section style={{ margin: 'auto 40px'  }}>
      <h1>Fulfill the form above</h1>

      <section>
        <SubscribeForm />
      </section>
    </section>
  )
}

export default Subscribe;
